{
  "name": "cypress-e2e",
  "version": "1.0.0",
  "description": "cypress",
  "main": "index.js",
  "scripts": {
    "test:addressBook": "npx cypress run --spec cypress/e2e/addressBook.cy.js",
    "test:homepage": "npx cypress run --spec cypress/e2e/homepage.cy.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "cypress": "^13.16.1",
    "cypress-mochawesome-reporter": "^3.8.2"
  }
}
